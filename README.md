# Project-Archiving-Utility
A PowerShell toolset to **bulk archive completed jobs** by moving job folders from an Active root (e.g. `P:\`) into an Archive root (e.g. `A:\`) using a **Projects CSV export** from ProWorkFlow.
